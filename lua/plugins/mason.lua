return {
  "williamboman/mason.nvim",
  opts = {
    mapping = {
      install_package = "I",
    },
  },
  keys = {
    -- Keymap to install the package under the current cursor position
    install_package = "I",
  },
}
